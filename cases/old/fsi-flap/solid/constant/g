FoamFile
{
    version     2.0;
    format      ascii;
    class       uniformDimensionedVectorField;
    object      g;
}

// Gravity disabled — bending is driven entirely by fluid forces
dimensions      [0 1 -2 0 0 0 0];

value           (0 0 0);
