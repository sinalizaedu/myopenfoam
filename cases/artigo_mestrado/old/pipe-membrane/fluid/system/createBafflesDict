FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createBafflesDict;
}

// Convert the internal face zone at x=1.0 into a cyclic baffle pair.
// membraneFront / membraneBack are referenced in 0/p as porousBaffle BC.

internalFacesOnly true;

baffles
{
    membrane
    {
        type        faceZone;
        zoneName    membraneZone;

        patches
        {
            master
            {
                name            membraneFront;
                type            cyclic;
                neighbourPatch  membraneBack;
            }
            slave
            {
                name            membraneBack;
                type            cyclic;
                neighbourPatch  membraneFront;
            }
        }
    }
}
